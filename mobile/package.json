{
  "name": "voice-garden-pocket",
  "version": "0.1.0",
  "private": true,
  "description": "Android-friendly voice training practice companion.",
  "type": "module",
  "scripts": {
    "prepare:web": "node scripts/prepare-capacitor.mjs",
    "cap:add:android": "npm run prepare:web && npx cap add android",
    "cap:sync": "npm run prepare:web && npx cap sync android",
    "apk:debug": "npm run prepare:web && npx cap add android && npx cap sync android && node scripts/patch-android.mjs && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
